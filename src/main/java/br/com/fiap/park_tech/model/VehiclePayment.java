package br.com.fiap.park_tech.model;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.Setter;
import org.springframework.data.annotation.Id;
import org.springframework.data.mongodb.core.mapping.Document;

import java.math.BigDecimal;
import java.time.Instant;
import java.time.LocalDateTime;

@AllArgsConstructor
@Getter
@Setter
@Document(collection = "vehicle_payments")
public class VehiclePayment {
    @Id
    private String id;
    private ParkingSession parkingSession;
    private BigDecimal amount;
    private LocalDateTime paymentDate;
    private Instant createdAt;
    private Instant updatedAt;
    private Instant deletedAt;

    public static VehiclePayment newVehiclePayment(ParkingSession parkingSession, BigDecimal amount, LocalDateTime paymentDate) {
        return new VehiclePayment(
                null, // id will be generated by MongoDB
                parkingSession,
                amount,
                paymentDate,
                Instant.now(), // createdAt
                Instant.now(), // updatedAt
                null // deletedAt
        );
    }
}
